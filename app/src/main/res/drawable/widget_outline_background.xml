<?xml version="1.0" encoding="utf-8"?>
<!--
  The widget's card: the app's usual pitch-black background with a crisp
  teal outline, as requested, instead of the app's red/gold accent colors —
  keeps the widget legible sitting on any home-screen wallpaper.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/bg" />
            <corners android:radius="@dimen/widget_corner_radius" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="4dp"
                android:color="@color/teal" />
            <corners android:radius="@dimen/widget_corner_radius" />
        </shape>
    </item>
</layer-list>
