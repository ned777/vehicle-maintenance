<?xml version="1.0" encoding="utf-8"?>
<!--
  The widget's own layout, fixed at 2x2 (see vehicle_widget_info.xml). Only
  one of allGoodText / itemsList is visible at a time —
  VehicleWidgetProvider.updateOneWidget() toggles which, and populates
  itemsList's rows (from widget_item_row.xml) in code.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_outline_background"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="10dp"
    android:paddingEnd="10dp">

    <!-- Which vehicle this widget instance was configured to watch. -->
    <TextView
        android:id="@+id/vehicleNameText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/accent"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textAllCaps="true"
        android:letterSpacing="0.04"
        android:singleLine="true"
        android:ellipsize="end" />

    <!-- Shown only when there is nothing overdue/due-soon, or on error. -->
    <TextView
        android:id="@+id/allGoodText"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:textColor="@color/ok"
        android:textSize="15sp"
        android:textStyle="bold"
        android:visibility="gone"
        android:text="" />

    <!-- Populated with up to two widget_item_row rows (the last one becomes
         a "Read more" link instead of an item once there's overflow). -->
    <LinearLayout
        android:id="@+id/itemsList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="4dp"
        android:orientation="vertical"
        android:gravity="center_vertical" />

</LinearLayout>
