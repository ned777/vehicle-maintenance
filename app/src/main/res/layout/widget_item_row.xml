<?xml version="1.0" encoding="utf-8"?>
<!--
  One row: a colored status dot, a category label, and its mileage-remaining
  detail underneath. Built repeatedly (once per fixed category) in
  VehicleWidgetProvider.buildHighlightRow() and added to itemsList via
  RemoteViews.addView().
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="1dp"
    android:paddingBottom="1dp">

    <ImageView
        android:id="@+id/ledDot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:src="@drawable/widget_led_ok" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:orientation="vertical">

        <!-- includeFontPadding="false" strips the extra ascent/descent
             padding Android normally reserves around text — with four rows
             stacked in a fixed-height widget, that padding was enough on
             its own to push the last row (Spark Plugs) past the bottom
             edge and get clipped. -->
        <TextView
            android:id="@+id/itemLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:singleLine="true"
            android:ellipsize="end"
            android:text="" />

        <TextView
            android:id="@+id/itemDetail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:includeFontPadding="false"
            android:textColor="@color/text_dim"
            android:textSize="11sp"
            android:singleLine="true"
            android:ellipsize="end"
            android:visibility="gone"
            android:text="" />

    </LinearLayout>

</LinearLayout>
